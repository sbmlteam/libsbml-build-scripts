# Generic Windows/Python Utility

Create full Python source package from CMAKE generated Python source bundle. Requires Python, Python-setuptools, cmake and tar to be in the path.

## Run location
<standalone>
